kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: ephemeral-test
nodes:
  - role: control-plane
  - role: worker
    labels:
      workload-type: ephemeral
  - role: worker
    labels:
      workload-type: persistent
